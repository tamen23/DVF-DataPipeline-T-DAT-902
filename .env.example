# --- PostgreSQL (serving database) ---
POSTGRES_USER=homepedia
POSTGRES_PASSWORD=homepedia
POSTGRES_DB=homepedia

# --- Airflow metadata database (created inside the same Postgres) ---
AIRFLOW_DB_USER=airflow
AIRFLOW_DB_PASSWORD=airflow
AIRFLOW_DB_NAME=airflow

# --- Airflow admin account ---
AIRFLOW_ADMIN_USER=admin
AIRFLOW_ADMIN_PASSWORD=admin

# --- MinIO (data lake) ---
MINIO_ROOT_USER=homepedia
MINIO_ROOT_PASSWORD=homepedia123
LAKE_BUCKET=lake

# --- MongoDB ---
MONGO_USER=homepedia
MONGO_PASSWORD=homepedia

# --- DVF years to ingest (comma-separated) ---
DVF_YEARS=2024
